# Kroger Developer API — https://developer.kroger.com (Production app, scope product.compact)
KROGER_CLIENT_ID=
KROGER_CLIENT_SECRET=
# Unset = SQLite at data/observatory.db (Phase 0). Postgres before Phase 1:
# DATABASE_URL=postgresql://user:pass@host:5432/observatory

# Optional. Free key from https://data.bls.gov/registrationEngine/ raises the BLS API limit from 25 to 500 queries/day.
BLS_API_KEY=
